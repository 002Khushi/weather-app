name: Hello Github Actions

on:
  push:
    branches: [develop]

    jobs:
     say_hello:
        runs-on: ubuntu-latest
        steps :
            - name: Print Greeting
              ruun: echo "Hello, from Github Action!"
